name: my_life
description: Project for vvsu

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 3.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^6.1.2
  path_provider: ^2.0.1
  hive: ^2.0.0
  hive_flutter: ^1.0.0
  intl: ^0.17.0
  font_awesome_flutter: ^9.0.0
  flutter_iconpicker: ^3.0.2
  decorated_icon: ^1.1.0
  http: ^0.13.1
  syncfusion_flutter_gauges: ^19.1.55
  expandable: ^5.0.1
  quiver: ^3.0.1
  flutter_animation_progress_bar: ^2.0.0
  flutter_slidable: ^0.6.0
  dio: ^4.0.0

  table_calendar: ^2.3.3

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.0.0
  build_runner: ^2.0.4

flutter:

  uses-material-design: true

  assets:
    - assets/logo/logo.png

